[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Vite 7 yêu cầu Node >= 20.19 / 22.12
  NODE_VERSION = "22"

# SPA fallback: react-router (createBrowserRouter) cần mọi route trả về index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
